import styled from 'styled-components';

const StyledCountDownButton = styled.a`
	padding-top: 10px;
	border: none;
	background-color: transparent;
	cursor: pointer;
`;

export default StyledCountDownButton;
