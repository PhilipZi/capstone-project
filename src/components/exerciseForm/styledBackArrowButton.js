import styled from 'styled-components';

const StyledBackArrowButton = styled.button`
	padding-left: 15px;
	border: none;
	background-color: transparent;
	cursor: pointer;
`;

export default StyledBackArrowButton;
